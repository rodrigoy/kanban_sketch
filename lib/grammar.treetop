grammar KanbanDSL

  rule stages
    (name (cards / substages) ';')*
  end

  rule substages
    '[' name cards ']'
  end
  
  rule cards
    '(' (name [,]?)+ ')'
  end

  rule name
    [a-zA-Z0-9\s]+
  end

end
